# AI 智能试衣项目启动说明

## 项目位置

项目位于：

```text
D:\AI 服装4
```

## Windows 启动方法（推荐）

1. 打开 `D:\AI 服装4` 文件夹。
2. 双击运行 `start.bat`。
3. 保持弹出的命令窗口开启，不要关闭。
4. 查看窗口中显示的访问地址，通常为：

   ```text
   http://127.0.0.1:8788/
   ```

5. 将该地址复制到 Chrome、Edge 等浏览器中打开。

如果 `8788` 端口已被占用，启动程序会自动选择下一个可用端口，例如 `8789`。请以命令窗口实际显示的地址为准。

## 正确的页面流程

从网站根地址进入后，页面顺序为：

```text
方案介绍首页 → 3D 试衣 → 动态试穿与环境融合
```

- `index.html`：方案介绍页，也是整个项目的正确入口。
- `poc/3d-fitting.html`：3D 智能试衣。
- `poc/scene3.html`：动态试穿与环境融合（当前复用原动态场景演示）。

请不要直接打开 `poc/3d-fitting.html`，否则会跳过方案介绍页。

## 停止运行

回到启动时弹出的命令窗口，按下 `Ctrl + C`，即可停止本地服务。停止后可以关闭命令窗口。

## 常见问题

### 双击 `start.bat` 后提示找不到 Python

需要先安装 Python，并在安装时勾选“Add Python to PATH”，然后重新运行 `start.bat`。

### 页面打不开

- 确认启动命令窗口仍然保持开启。
- 使用命令窗口中实际显示的地址，不要固定使用某个端口。
- 不要直接双击 HTML 文件运行，建议始终通过 `start.bat` 启动。

